{
  "name": "leave-management-api",
  "version": "1.0.0",
  "description": "Mock backend using json-server for Leave Management सिस्टम",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}